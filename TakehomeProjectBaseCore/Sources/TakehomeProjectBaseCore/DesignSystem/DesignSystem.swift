import CoreGraphics

public struct DesignSystem { }

extension DesignSystem {
  public struct Colors {
    private init() { }
    
    static let text = CGColor(red: 0, green: 0, blue: 0, alpha: 1)
  }
}
